# V2 Bird Hub

This is the new `bird-hub` workspace.

It is the future central aggregation and visualization server. For now it contains a minimal installable scaffold so the root installer can already target the `v2 bird-hub` variant.
